# Frontend (Vite) — required, exposed to the browser
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Backend (Supabase Edge Function secrets) — never expose to the frontend
# Managed in Lovable Cloud / Supabase project secrets, NOT in this file.
# LOVABLE_API_KEY=
